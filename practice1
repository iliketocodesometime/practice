# Filter potential FAs where 'Branch City' contains the closest city
        potential_FAs = potential_FAs[potential_FAs['Branch City'].str.upper().str.contains(closest_city.upper())]
        
        # Ensure there are still potential FAs left after filtering
        if not potential_FAs.empty:
            # Sort potential FAs by distance, then by DT2_STRT
            chosen_FA = potential_FAs.sort_values(by=['distance', 'Date Posted']).iloc[0]
